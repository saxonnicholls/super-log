# assets

Screenshots and images used by the README.

Captured from a deliberately clean bench: no production hosts, no real
hostnames, no home-directory paths. The streams are the demo clients plus a
few of the watchers, all started with commands that appear in the README.

If you retake them, check the frame for machine names, usernames in file
paths, LAN addresses and anything from a real server before committing.
