{
  "name": "@super-log/client",
  "version": "0.1.0",
  "private": true,
  "description": "Forward console and structured logs to a super-log hub - React Native, browser, Node",
  "author": "Saxon Herschel Nicholls",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "//prepare": "main points at dist/, which is a build artefact and not in git. npm runs prepare when this package is installed as a file:/git dependency, so a consumer gets a built package instead of an entry point that does not exist.",
    "prepare": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
