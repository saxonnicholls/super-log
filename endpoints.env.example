# Written by superlog-alarm - the URLs this gateway currently owns.
# Source it (`. endpoints.env`) or read it; rewritten on every change.
# This example shows the shape; the real file is gitignored because the
# URLs are ephemeral and semi-sensitive (an unguessable hostname IS the
# secrecy of a capture endpoint).
ALARM_URL=https://alarm.example.com
DEMO_URL=https://adjective-noun-noun-noun.trycloudflare.com/hook/demo
STRIPE_TEST_URL=https://adjective-noun-noun-noun.trycloudflare.com/hook/stripe-test
